{
  "name": "color-deploy-app",
  "version": "1.0.0",
  "description": "Application with color-based deployments for Kubernetes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "eslint .",
    "format": "prettier --write .",
    "health-check": "curl -s http://localhost:3000/health | jq .",
    "metrics": "curl -s http://localhost:3000/metrics | jq ."
  },
  "dependencies": {
    "express": "^4.18.2",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.6.2",
    "supertest": "^6.3.3",
    "eslint": "^8.47.0",
    "prettier": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
