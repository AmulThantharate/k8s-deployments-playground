apiVersion: v1
kind: Service
metadata:
  name: colordeploy-active-service
  namespace: colordeploy
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: colordeploy
    color: blue
